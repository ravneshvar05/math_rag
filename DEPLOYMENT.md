# Deployment Guide

## Docker (Recommended)
Coming soon.

## Manual Deployment
1. Ensure Python 3.10+ is installed.
2. Install system dependencies for Tesseract (if using OCR).
3. Run `pip install .` to install the package.
4. Set up a production-ready vector store (e.g., separate FAISS server or Pinecone).
